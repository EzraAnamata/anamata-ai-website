# anamata.ai website — repository instructions

## Commit identity (repository convention — overrides any global commit-identity policy)

By explicit owner (Ezra Hulsman) directive, 2026-07-07: **AI-authored changes
in THIS repository are committed under the AI identity, not a human name.**

- Author for AI work: `Otto <otto@anamata.ai>` — already set in this
  checkout's local git config, with `commit.gpgsign false`. Do not "fix"
  either setting.
- Commit messages stay clean Conventional Commits, ≤72 chars, no
  `Co-Authored-By`/`Generated by` trailers — the AUTHOR field is the
  visibility mechanism, not the message.
- **Signed = human, unsigned Otto = AI.** Humans act via GitHub-signed PR
  merges and named deployment approvals (or their own GPG-signed commits).
  Never sign an Otto commit; never author AI work under a human name here.
- Full convention and the pre-`b4babc4` history caveat: `docs/authorship.md`.

This is a deliberate, documented exception to the global "no AI attribution"
commit policy, scoped to this repository only — the site's product IS its
AI-built provenance. Other repos (including anamata-ai-internal) keep the
global policy.

## Process (unchanged)

- All changes ship via PR → human merge → gated deploy (`production`
  environment). Never push to main; never bypass the gate.
- Tests first (`tests/launch-checks.test.mjs`), tokens are law
  (`design-tokens.json` → generated CSS), the ledger is never fabricated.
- Continuation docs: private repo `EzraAnamata/anamata-ai-internal`
  (handoff, direction note, executable specs).
